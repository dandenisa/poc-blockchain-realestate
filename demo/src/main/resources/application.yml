spring:
  application:
    name: poc-blockchain-realestate  #current service name to be used by the eureka server

server:
  port: 9099    #default port where the service will be started

web3j:
  client-address: http://127.0.0.1:7545

management:
  security:
    enabled: false  #disable the spring security on the management endpoints like /env, /refresh etc.

logging:
  level:
    com.example.demo1: DEBUG


